import "$sitb/entities/intl";
/**
 * Fake file to avoid infinite loop in rollup `analyse` post-build phase, between
 * `building SSR bundle for production...`
 * and
 * `building for production...`
 */